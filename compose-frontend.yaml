services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: frontend
    ports:
      - "127.0.0.1:8501:8501"
    restart: unless-stopped

    environment:
      SERVER_HOST: server
      SERVER_PORT: 8080

[project]
name = "frontend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "httpx",
    "os-chatbot-utils",
]

[tool.uv]
package = false

[tool.uv.sources]
os-chatbot-utils = { path = "../utils" }

[tool.ty.environment]
python = ".venv"
